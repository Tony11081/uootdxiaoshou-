AUTH_JWT_SECRET=CHANGE_ME_LONG_RANDOM
ADMIN_EMAIL=chengyadong1112@gmail.com
ADMIN_PASSWORD_HASH=CHANGE_ME_ARGON2_HASH
ADMIN_TOTP_SECRET=CHANGE_ME_TOTP_SECRET

# OpenRouter key for google/gemini-2.5-flash
OPENROUTER_API_KEY=CHANGE_ME_OPENROUTER_KEY

# Optional fallback Gemini key
GEMINI_API_KEY=

# Vercel KV (persistent order storage). On Vercel, create a KV database and it will auto-inject these.
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Alternative (Upstash Redis REST envs). If you connect Upstash, you can set these instead.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: embed the external product catalog via a same-site subdomain proxy.
# Recommended for iOS/Safari and browsers that block third-party iframe cookies.
# Example: https://catalog.newuootd.com
NEXT_PUBLIC_CATALOG_EMBED_ORIGIN=
